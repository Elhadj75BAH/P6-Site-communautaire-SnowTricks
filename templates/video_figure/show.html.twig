{% extends 'base.html.twig' %}

{% block title %}VideoFigure{% endblock %}

{% block body %}
    <h1>VideoFigure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video_figure.id }}</td>
            </tr>
            <tr>
                <th>Video</th>
                <td>{{ video_figure.video }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('video_figure_index') }}">back to list</a>

    <a href="{{ path('video_figure_edit', {'id': video_figure.id}) }}">edit</a>

    {{ include('video_figure/_delete_form.html.twig') }}
{% endblock %}
