<form method="post" action="{{ path('video_figure_delete', {'id': video_figure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video_figure.id) }}">
    <button class="btn">Delete</button>
</form>
